# ==================================================
# zsh-abbr abbreviations (session scope, version-controlled)
# ==================================================

# dotenvx: run a command with env loaded quietly
abbr -S -q add "dx=dotenvx run -q --"

# docker compose
abbr -S -q add "fig=docker compose"
