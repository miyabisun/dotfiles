---
name: spike
description: >-
  黎明期の最速試作。まず動かして画期的な体験を得ることだけを目的に、レビュー・
  formatter ゲート・セキュリティゲート・知識棚卸しを全て省いて実装し、動作証拠
  1つと local commit で完結する。user が明示的に spike を起動したとき、または
  同じ依頼で黎明期段階を明示したときだけ使う。$deliver からの暗黙選択は禁止。
  push・deploy・release はしない。
---

# spike

目的はただ一つ: **最短で動くものを作り、体験を得る**。品質はあとの `polish` が、
堅牢性は `harden` が引き受ける (decision 0002)。8割未満で止めてよい。TODO を
残してよい。完璧さをこのスキルに持ち込んだ時点で失敗である。

## 起動条件

user の明示的な `$spike` 起動、または同じ依頼文での段階明示だけが起動根拠。
段階未指定の `$deliver` からこのスキルを推論で選んではならない (保証の暗黙降格
禁止)。

## 手順

1. **契約は自分で決める**: 達成条件は最大3行。「何が動けば体験できたことに
   なるか」だけを書く。ledger・counterpart 照会・独立提案交換は行わない。
2. **作る**: 最小の動くものを最短経路で。既存コードの流儀に合わせるのは
   「読める程度」で足りる。抽象化・設定項目・将来対応は作らない。
3. **動かす**: 動作証拠を1つ取る — 実行コマンドと出力、または実際の操作結果。
   これが唯一の必須証拠であり、テスト作成は任意 (書くなら happy path 1本まで)。
   変更した領域に既存テストがあり数秒で走るなら実行し、壊したままにしない。
4. **コミットする**: 既定は 1 invocation = 1 local commit。複数 checkpoint
   commit は、起動時の user 依頼文が明示的に許可した場合のみ (その原文を
   receipt に引用し、件数と各 scope を報告する)。English Conventional Commits。
5. **報告する**: 何が動くか、動作証拠、残した TODO、次に polish すべき点を
   短く返す。

## 昇格トリガー (強制)

次に触れる必要が生じたら、このスキルの内側で続行せず、**harden への切り替えを
宣言して停止する**。推奨で済ませてはならない。

- credential・secret・`.env` 系の取り扱い
- 権限・認証・信頼境界の変更
- 破壊的なデータ操作 (drop・一括削除・migration)
- 外部公開・release artifact・第三者へ届く出力

閉域 LAN での agent 間リスクの受容 (decision 0002, user-origin) はこの列挙を
免除しない。受容されたのは lateral agent takeover だけである。

## 不変条件 (全段階共通)

- push・merge・deploy・release はしない
- secret・`.env` をコミットしない。agent-talk journal に秘密を載せない
- 無関係な作業中変更 (他セッションの未コミット作業を含む) を保護する
- 破壊的 git 操作 (checkout/restore/reset/clean/stash) で作業を管理しない
- 既存の有効なテストを green にするための改変・削除をしない
